@model IList<Akce>

@{
    ViewData["Title"] = "Databáze Akcí";
}
<h1>@ViewData["Title"]</h1>

<a asp-action="Create">Create Product</a>

@{
    if (Model != null && Model.Count > 0)
    {
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">@nameof(Akce.Id)</th>
                    <th scope="col">@nameof(Akce.Name)</th>
                    <th scope="col">@nameof(Akce.Description)</th>
                    <th scope="col">@nameof(Akce.Price)</th>
                    <th scope="col">@nameof(Akce.ImageSrc)</th>
                    <th scope="col">Delete</th>
                    <th scope="col">Edit</th>
                </tr>
            </thead>
            <tbody>
                @for (int i = 0; i < Model.Count; ++i)
                {
                    <tr>
                        <th scope="row">@Model[i].Id</th>
                        <td>@Model[i].Name</td>
                        <td>@Model[i].Description</td>
                        <td>@Model[i].Price</td>
                        <td>@Model[i].ImageSrc</td>
                        <td><a asp-action="Delete" asp-route-Id="@Model[i].Id" onclick="return ConfirmDelete();">Delete</a></td>
                        <td><a asp-action="Edit" asp-route-Id="@Model[i].Id">Edit</a></td>
                    </tr>
                }
            </tbody>
        </table>

    }
}

@section Scripts
{
    <script src="~/js/dialogs.js"></script>
}